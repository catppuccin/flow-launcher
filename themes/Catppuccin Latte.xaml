<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:system="clr-namespace:System;assembly=mscorlib">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Themes/Win11Light.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <Style
        x:Key="ItemBulletSelectedStyle"
        BasedOn="{StaticResource ItemBulletSelectedStyle}"
        TargetType="{x:Type Border}">
        <Setter Property="Background" Value="#8839ef" />
    </Style>
    <Style
        x:Key="ItemGlyph"
        BasedOn="{StaticResource ItemGlyph}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#6c6f85" />
    </Style>
    <Style
        x:Key="QueryBoxStyle"
        BasedOn="{StaticResource QueryBoxStyle}"
        TargetType="{x:Type TextBox}">
        <Setter Property="Foreground" Value="#4c4f69" />
        <Setter Property="CaretBrush" Value="#4c4f69" />
    </Style>
    <Style
        x:Key="QuerySuggestionBoxStyle"
        BasedOn="{StaticResource QuerySuggestionBoxStyle}"
        TargetType="{x:Type TextBox}">
        <Setter Property="Foreground" Value="#6c6f85" />
    </Style>

    <Style
        x:Key="WindowBorderStyle"
        BasedOn="{StaticResource WindowBorderStyle}"
        TargetType="{x:Type Border}">
        <Setter Property="BorderBrush" Value="#ccd0da" />
        <Setter Property="Background" Value="#e6e9ef" />
    </Style>

    <!--  Item Style  -->
    <Style
        x:Key="ItemTitleStyle"
        BasedOn="{StaticResource ItemTitleStyle}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#5c5f77" />
    </Style>
    <Style
        x:Key="ItemSubTitleStyle"
        BasedOn="{StaticResource ItemSubTitleStyle}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#6c6f85" />
    </Style>
    <Style
        x:Key="SeparatorStyle"
        BasedOn="{StaticResource SeparatorStyle}"
        TargetType="{x:Type Rectangle}">
        <Setter Property="Fill" Value="#acb0be" />
    </Style>
    <Style
        x:Key="ItemTitleSelectedStyle"
        BasedOn="{StaticResource ItemTitleSelectedStyle}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#4c4f69" />
    </Style>
    <Style
        x:Key="ItemSubTitleSelectedStyle"
        BasedOn="{StaticResource ItemSubTitleSelectedStyle}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#5c5f77" />
    </Style>
    <SolidColorBrush x:Key="ItemSelectedBackgroundColor">#ccd0da</SolidColorBrush>


    <!--  button style in the middle of the scrollbar  -->
    <Style
        x:Key="ThumbStyle"
        BasedOn="{StaticResource ThumbStyle}"
        TargetType="{x:Type Thumb}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border
                        Background="#ccd0da"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        CornerRadius="2"
                        DockPanel.Dock="Right" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style
        x:Key="SearchIconStyle"
        BasedOn="{StaticResource SearchIconStyle}"
        TargetType="{x:Type Path}">
        <Setter Property="Fill" Value="#7c7f93" />
    </Style>
    <Style x:Key="SearchIconPosition" BasedOn="{StaticResource SearchIconPosition}" TargetType="{x:Type Canvas}">
        <Setter Property="Background" Value="#e6e9ef" />
    </Style>

    <Style x:Key="ItemHotkeyStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#9ca0b0" />
    </Style>
    <Style x:Key="ItemHotkeySelectedStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#8839ef" />
    </Style>
    <Style x:Key="ItemGlyphSelectedStyle" BasedOn="{StaticResource ItemGlyphSelectedStyle}" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#4c4f69" />
    </Style>
    <Style
        x:Key="ClockBox"
        BasedOn="{StaticResource ClockBox}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#6c6f85" />
    </Style>
    <Style
        x:Key="DateBox"
        BasedOn="{StaticResource DateBox}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#6c6f85" />
    </Style>
    <Style
        x:Key="PreviewBorderStyle"
        BasedOn="{StaticResource PreviewBorderStyle}"
        TargetType="{x:Type Border}">
        <Setter Property="BorderBrush" Value="#e6e9ef" />
    </Style>
    <Style
        x:Key="PreviewItemTitleStyle"
        BasedOn="{StaticResource PreviewItemTitleStyle}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#1e66f5" />
    </Style>
    <Style
        x:Key="PreviewItemSubTitleStyle"
        BasedOn="{StaticResource PreviewItemSubTitleStyle}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#bcc0cc" />
    </Style>
    <Style
        x:Key="PreviewGlyph"
        BasedOn="{StaticResource PreviewGlyph}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#1e66f5" />
    </Style>
</ResourceDictionary>
